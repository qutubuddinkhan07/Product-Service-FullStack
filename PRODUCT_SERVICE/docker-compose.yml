version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - product-inventory-system-db-data:/var/lib/mysql

volumes:
  product-inventory-system-db-data:
